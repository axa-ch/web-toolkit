# language setting
language: node_js

# version numbers, testing against two versions of node
node_js:
  - 0.10.25

before_install:
  - npm install -g gulp

script:
  - npm run gulp build
  - npm run gulp deploy

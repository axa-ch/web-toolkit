# language setting
language: node_js

env:
  - REPO_URL=git@github.com:axa-ch/style-guide.git

# version numbers, testing against two versions of node
node_js:
  - 0.10.25

before_install:
  - npm install -g gulp

script:
  - gulp build
  - if [ "$BRANCH" = "master" ]; then gulp deploy; fi

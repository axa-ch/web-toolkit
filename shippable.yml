language: node_js

node_js:
  - 0.10.25

env:
  - REPO_URL=git@github.com:axa-ch/style-guide.git GITHUB_INTEGRATION=true

before_install:
  - npm install -g gulp

script:
  - gulp build
  - if [ "$PULL_REQUEST" = false ] && [ "$BRANCH" = "develop" ]; then gulp deploy; fi

---
title: Mixins
slug: mixins
template: page.jade
---

.masthead.masthead--docs
  .l-container
    p.lead.masthead__lead.
      Mixins to keep your less aligned to the Style Guide.

.l-container
  .l-row
    .l-full
      h1 Mixins

  .l-row
    .l-primary
      each mixin in mixins
        div.block
          h2(id=mixin.slug)= mixin.title
            a(href='#' + mixin.slug).anchor--docs
          div.block!= mixin.contents

    .l-secondary
      nav.nav.nav--side(data-spy="affix", data-offset-top="230")
        ul.menu.menu--side
          each mixin in mixins
            li.menu__item
              a.menu__link(href='#' + mixin.slug)= mixin.title
